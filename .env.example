# Copy this file to .env and fill in your values

# Required for real AI evaluation
OPENROUTER_API_KEY=sk-or-xxxxxxxxxxxxxxxxxxxxxxxx
OPENROUTER_MODEL=deepseek/deepseek-chat-v3.1:free
OPENROUTER_REFERRER=http://localhost

# Switch evaluator mode: mock | openrouter
EVAL_MODE=openrouter

# Server
PORT=3000
COOLDOWN_MS=30000

# Database (optional; if omitted, in-memory store is used)
# Local Postgres example (no SSL):
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/language_duel
# PGSSL=0

# Google Login (optional)
# Obtain a Client ID from Google Cloud Console (OAuth 2.0 / Web)
# GOOGLE_CLIENT_ID=xxxxxxxxx-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com
# AUTH_SECRET is not required for the current in-memory session flow, but can be used later for JWTs
# AUTH_SECRET=change-me
